# Original source archives

This directory preserves the exact recovered source ZIP files without normalization. Use the extracted repository tree for development; use these archives for provenance and historical comparison.

Both v2.1.0 files are intentionally present because they are distinct artifacts.
